const { FS } = require('../../mocks')

const buildConstants = require('../buildConstants')

describe('Build Constants', () => {
  
  beforeEach(() => {

  })
  
  it('', () => {
    
  })

  it('', () => {
    
  })
})
